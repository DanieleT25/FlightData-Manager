# Copy this file to .env and fill in the value, then source it before registering
# the runner: set -a; source .env; set +a
# Generate the token at:
#   Gitea UI → Site Administration → Actions → Runners → Create new runner
GITEA_RUNNER_TOKEN=<paste-token-from-gitea-ui>
